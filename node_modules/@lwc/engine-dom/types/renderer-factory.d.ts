/**
 * Note: This module cannot import any modules because it is meant to be a pure function.
 *   Type-Only imports are allowed.
 */
import type { RendererAPI } from '@lwc/engine-core';
/**
 * A factory function that produces a renderer.
 * Renderer encapsulates operations that are required to render an LWC component into the underlying
 * runtime environment. In the case of @lwc/enigne-dom, it is meant to be used in a DOM environment.
 * Example usage:
 * import { renderer, rendererFactory } from 'lwc';
 * const customRenderer = rendererFactory(renderer);
 *
 * @param baseRenderer Either null or the base renderer imported from 'lwc'.
 */
export declare function rendererFactory(baseRenderer: RendererAPI | null): Omit<RendererAPI, 'insertStylesheet' | 'isNativeShadowDefined' | 'isSyntheticShadowDefined'>;
